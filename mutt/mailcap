text/html;      ~/bin/view-attachment %s html;     test=test -n "$DISPLAY"
text/html;      w3m %s;     nametemplate=%s.html;       needsterminal
text/html;      w3m -v -I %{charset} -F -T text/html -dump %s;        copiousoutput
text/calendar;  khal import -a personal %s;

